# headless-preview-graphql
A reusable WordPress plugin that lets a headless frontend preview draft content through WPGraphQL using a shared secret. Includes a dedicated resolver to preview draft WooCommerce products, bypassing WooGraphQL's publish-only loader.
